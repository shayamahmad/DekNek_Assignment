# Dev: leave unset — Vite proxies /api to the server on localhost:5000.
# Production (e.g. Vercel): required — your deployed API base URL, no trailing slash (set in Vercel env before build).
# VITE_API_URL=https://your-api.onrender.com
