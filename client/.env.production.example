# Used when building for production (e.g. Vercel). Set in the host UI — do not commit real values.
# Your deployed Express API base URL (Render, Railway, etc.), no trailing slash.
# VITE_API_URL=https://auth-api-xxxx.onrender.com
